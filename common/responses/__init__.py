"""Response helpers.

Intentionally minimal. The project's 242 endpoints return several different
body shapes and a released mobile client depends on them, so a unified response
envelope is a breaking change tracked in ``docs/api.md`` rather than something
this package imposes.
"""
