# Development and CI dependencies. Not installed into the production image.
-r requirements.txt

pytest==8.3.3
pytest-django==4.9.0
pytest-cov==7.1.0

# In-memory Redis stand-in for infrastructure/keys tests -- avoids requiring
# a live Redis server (and its native Lua/EVAL support) just to run the test
# suite. See tests/unit/test_key_management.py.
fakeredis==2.37.0

# Lint + format + import sorting + basic security checks, in one tool.
ruff==0.6.9
